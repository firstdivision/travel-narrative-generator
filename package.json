{
  "name": "travel-journal-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-photo-indexes.js && vite",
    "build": "node scripts/generate-photo-indexes.js && vite build",
    "preview": "vite preview",
    "manifest": "node scripts/generate-narrative-manifest.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "dompurify": "^3.2.6",
    "marked": "^15.0.12"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.3.5"
  }
}