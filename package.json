{
  "name": "travel-journal-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-photo-indexes.js && node scripts/generate-narrative-manifest.js && vite",
    "build": "node scripts/generate-photo-indexes.js && node scripts/generate-narrative-manifest.js && vite build",
    "preview": "vite preview",
    "manifest": "node scripts/generate-narrative-manifest.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "dompurify": "^3.2.6",
    "marked": "^15.0.12"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^26.1.0",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}